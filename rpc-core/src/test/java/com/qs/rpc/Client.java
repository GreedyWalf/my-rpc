package com.qs.rpc;

import com.qs.rpc.core.RPC;
//import com.qs.rpc.serviceImpl.UserService;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

//@RunWith(SpringJUnit4ClassRunner.class)
//@ContextConfiguration(locations = {"classpath:serviceImpl-provider.xml"})
public class Client {

//    @Test
//    public void start() {
//        long start = System.currentTimeMillis();
//        UserService userService = (UserService) RPC.call(UserService.class);
//        String userName = userService.getUserName("111", 20);
//        System.out.println(userName);
//        System.out.println("耗时：" + (System.currentTimeMillis() - start) + "ms");
//    }
}